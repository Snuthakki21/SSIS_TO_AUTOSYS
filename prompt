You are helping me write a T-SQL script for SQL Server 2022.
Context:  
I have a table with two relevant columns:
•	Primary key: `Id` (int or bigint).
•	String column: `Code`.
The base portion of `Code` is always exactly 26 characters starting at position 1, i.e. `SUBSTRING(Code, 1, 26)`.
Business rules:
1.	Define:
•	`BaseCode = SUBSTRING(Code, 1, 26)`.
•	A row is bad if:
•	`LEN(Code) > 26`, and
•	`SUBSTRING(Code, 27, 3) = '000'`.
•	A row is good if it has the same `BaseCode` but does not satisfy the “bad” condition.
2.	I only want to delete bad rows when BOTH are true:
•	There is at least one good row for that `BaseCode`.
•	There is at least one bad row for that same `BaseCode`.
What I need:
1.	Use CTEs to keep it readable:
•	CTE `Normalized` that returns:
•	Id
•	Code
•	BaseCode = SUBSTRING(Code, 1, 26)
•	IsBad = 1 if `LEN(Code) > 26 AND SUBSTRING(Code, 27, 3) = '000'`, else 0.
•	CTE `HasGoodAndBad` grouped by `BaseCode` that keeps only those BaseCodes where:
•	Count of good rows > 0, and
•	Count of bad rows > 0.
2.	Output three queries:
a) A SELECT that returns `COUNT(*)` of rows that would be deleted (only bad rows whose BaseCode appears in `HasGoodAndBad`).  
b) A SELECT preview that returns the detailed rows that would be deleted (Id, Code, BaseCode, etc.).  
c) A DELETE statement that:
•	Deletes from `dbo.YourTable` aliased as `t`.
•	Joins `t` to `Normalized` on Id and to `HasGoodAndBad` on BaseCode.
•	Deletes only rows where `IsBad = 1`.
3.	General requirements:
•	Assume table name `dbo.YourTable`, PK `Id`, string column `Code`.
•	Use concise, clear CTEs and joins.
•	Add short inline comments explaining the main conditions (BaseCode, IsBad, and why we only delete when both good and bad exist).
Generate complete T-SQL code I can run directly.